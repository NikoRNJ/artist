## Próximos pasos (roadmap sugerido)

- Sustituir mocks por API real (REST/GraphQL) y persistencia (DB).
- Autenticación (cliente/artista) + roles y permisos.
- Integración de pagos real (Stripe) y calendar sync (Google/ICS).
- Routing (React Router) y code-splitting por vista/feature.
- Validación y formularios (React Hook Form + Zod), tests (Vitest/RTL).
